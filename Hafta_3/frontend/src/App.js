import Form from "./components/form/Form";

function App() {
  return (
    <div
      style={{
        alignItems: "center",
        display: "flex",
        justifyContent: "center",
      }}
    >
      <Form></Form>
    </div>
  );
}

export default App;
